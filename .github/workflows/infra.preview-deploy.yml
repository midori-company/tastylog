name: 'infra preview & deploy'

on:
  push:
    branches:
      - master
    paths:
      - 10_infra/**
      - .github/workflows/**

permissions:
  id-token: write
  contents: read

jobs:
  preview:
    name: 'preview'
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Hello World"
